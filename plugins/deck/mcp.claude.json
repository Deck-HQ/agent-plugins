{
  "mcpServers": {
    "deck": {
      "type": "http",
      "url": "${DECK_MCP_URL:-https://usedeck.ai/api/v1/mcp}",
      "headers": {
        "Authorization": "Bearer ${DECK_API_KEY}"
      }
    }
  }
}
